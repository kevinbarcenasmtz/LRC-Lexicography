# Memory Index

- [Burrow parser — current status](project_burrow_parser_status.md) — Ko./Pattern-E/Tamil-missing/Naiki/Ma-type/Kuwi-type/leading-qualifier/headword-qualifier-after-lang-marker/nested-tag-headword-bold-bcf (791f89b)/To.-type-bold (ee716f1, +181)/single-char-vowel-headword (fc3104b, +23 slots)/DED-1-Ta Pattern-G (fae4cca)/To.-type bold-embedded-marker behind sci-names (44287fc, +221, `<b>`-anchor guard vs DED 814 elided-gloss) all closed; open: To.-type TEXT variant RUNNING-TEXT sub-case only (no `<b>` before `<i>`, incl. DED 5154's own To. + elided-form DED 814 — needs a headword-quality guard the `<b>` anchor can't provide), Kui bare sense-suffix forms, DED 410/4896 dup entries
- [Triage-ded ledger status](project_triage_ded_ledger_status.md) — match rate **98.5%** (19,075; Language-only **144**, No **135**) after 2026-08-20 **Lane-A recoverable-candidate sweep = 4 matcher fixes shipped, 0 net regressions** (dravidilex-pilot, all pushed): (1) **comma/slash multiform-delimiter fold** 7534128 (+4); (2) **split-token atom-exact match** 384d8dd (matcher-side `starling_tree_validator._match_entry`: also accept a headword match when any `/`-delimited ATOM of the Starling form exactly equals a Burrow token atom — Burrow SPLITS multiform headwords into single tokens (Ma. `a, ā`→`['a','ā']`) the whole-string/substring tests miss under the ≥2 length guard; exact full-atom equality only; +3 all LO→Yes: DED 1+410 Malayalam `a, ā`/`i, ī` demonstrative bases, 4572 Tamil `pō`; No frozen 135; ledger `split-token-atom-exact-match`); (3) **`<->` scrape-artifact strip** 43259ca (matcher-side `textnorm.normalize_for_match_variants` strips `\s*<->\s*` before the optional-infix search — the artifact sat after `(r)` blocking `b(r)atuku` expansion; +1 DED 5372 Telugu `bratuku`; ledger `arrow-artifact-strip`; NOTE: display artifact remains in 3 baked corpus headwords, parser could clean at source later); (4) **space-insensitive EXACT match** d166c4f (matcher-side `_match_entry`: also accept when the two normalized forms are equal with ALL spaces removed — full-string equality ONLY, never substring; reconciles OCR-split words (Kuwi `sap ta`=`sapta`), intra-paren spacing (`kal (obl. kad-, pl. kalk)`=`kal (obl.kad-,pl.kalk)`), spaced optional-paren (`nére(h)`=`nēreh`), spaced half-length dot (Toda `pu · f`=`pūf`); +6 LO→Yes: DED 2331 Kuwi, 5151 Malto, 1298 Muria+Maria Gondi, 4345+4932 Toda; ledger `despaced-exact-match`; **SUPERSEDES the reverted paren-space attempt — recovers the 2 DED 1298 rows via safe exact-equality with NO DED 410 Chanda regression**). Prior in-session: **comma/slash multiform-delimiter fold** (7534128, dravidilex-pilot, pushed; matcher-side/zero-regen: `textnorm.normalize_for_match` extends the slash-whitespace collapse to also fold the top-level comma delimiter to `/` (`\s*/\s*`→`\s*[,/]\s*`); Burrow joins multiform stems with `/` (irum/iṛum) while Starling comma-separates them (irum, iṛum); fold runs after in-paren commas are stripped so tense parens untouched, both-sided/order-preserving → merge-only; +4 all Language-only→Yes (19061→19065): DED 485 Muria+Chindwara Gondi, 2783 Betul Gondi, 3748 Toda; No frozen 135, Language-only 158→154, 0 regressions; ledger key `comma-slash-delimiter-fold`. **From Lane-A shared-shape sweep of the 158 Language-only rows — highest-count recoverable cluster.** **INTRA-PAREN-SPACE-STRIP candidate ATTEMPTED & REVERTED same session**: extending the in-paren comma-strip lambda to also `.replace(" ","")` recovered +5 (DED 1298×2 Muria/Maria, 5153 Inscr.Telugu, 5154 Toda, 5552 Kui — all differ only by intra-paren spacing) BUT tripped +1 Yes→LO regression on **DED 410 Chanda Gondi `ēr (pl. ērk)`** — its base `Yes(substring,0.95)` was a SPURIOUS match on the bare plural fragment `pl. ēṛ` borrowed from a different dialect's form (`ēl (obl. ēn-), pl. ēṛ`), and stripping Starling's `(pl. ērk)`→`pl.erk` dissolved that coincidental substring. Reverted per strict 0-regression rule (user's call); don't re-attempt without ALSO recovering 410 Chanda via a ChD./Chanda inline-form lever (the real form `(W. Ph. ChD.) ēr, pl. ērk` exists in Burrow but the dialect-scan misses it). Attribution proved by 3-way validator diff (BASE/comma-slash-only/both): comma-slash clean 0-regression, paren-space owns the 1 regression. **ALL recoverable candidates from the sweep CLOSED (fixes 2/3/4 above).** **GENUINE-DIVERGENCE LOGGING DONE (2026-08-20):** the 144 residual Language-only rows were classified into 6 categories (worksheet at `tree_validation_output/genuine_divergence_worksheet.csv`, gitignored) and **132 logged as language-scoped `genuine_divergence`** in review_ledger.json (also gitignored — nothing to commit): phonological/spelling divergence 76, distinct reflex 40, morphological citation (Gondi -ānā/Kuwi -ali vs bare stem) 13, Inscr.-Telugu ṛ/ḍ correspondence 3. Grouped shared note per category, tagged `[Lane-A grouped genuine-divergence pass]`, reviewed_by=claude-triage. DED 410 became a mixed whole-DED(parser_bug_fixed)+per-language(genuine) entry; DED 495 Adilabad + 9 Irula rows were pre-existing genuine (left alone) → ledger now 142 genuine_divergence records total. **HELD FOR REVIEW (not logged): 11 `display_fallback_or_postposed` rows** where the matcher shows an English gloss as the "Burrow form" because no headword matched (huppe→field-rat, kētul→hut, guḍḍī→tomb/temple, gumiya→grave, kohk-→thresh, vāŋg-→leak, eɵy→e) — several are POSTPOSED-inline-marker cases (`huppe (M.)` sits in the Go. gloss after the form) the matcher can't reach = a real OPEN LEAD (a postposed-marker matcher lever could recover some; the reverted 2026-08-19 muria postposed-backward-look attempt shows it's fragile — needs a careful forward-anchored approach). **NEXT: postposed-inline-marker lever for the 11 held rows, or session done.**). Prior match rate **98.5%** (19,061) after 2026-08-20 **Tamil comma-for-period marker** (766011a, dravidilex-pilot, pushed; **PARSER, CORPUS REGEN'D — RE-SYNC TO BOX**: new `_COMMA_MARKER_PATTERN` + gated loop for `<b><i>Ta</i>, par̤i…</b>` (DED 4002) where the abbrev's period is OCR'd as a comma outside the italic; Pattern A's `</i>\s+` guard can't admit it, so a separate pattern reconstructs the period + gates on `_is_known_lang_abbrev('Ta.')` (sci-names rejected); isolated diff exactly 1 entry/0 loss/+Ta.; +1 (19060→19061), No 136→135, Language-only frozen 158, 0 regressions; ledger key `tamil-comma-for-period-marker`. **Scattered-parser tail closed for the session — user's call: did the safe/clean ones (2690/3918/1563/5006/4143/4002 + Naiki 75/1623/430), DEFERRED 5 hard rows (4473 Malto/3535 Konda/379 Muria/4885 Konda/2116 Naiki) to manual Excel edit or genuine_divergence logging. NEXT: per-row divergence logging of ~46-lang Language-only spread (158)**). Prior **98.5%** (19,060) after 2026-08-20 **4 non-canonical-markup parser one-offs** (6f20bd2, dravidilex-pilot, pushed; **PARSER, CORPUS REGEN'D — RE-SYNC TO BOX**; from unmatched_diagnostics.md scattered-parser tail: (1) NEW Pattern CQ `<i><b>Lang.</b> (biblio-qual)</i> <b>hw</b>` citation-after-bold-abbrev-inside-italic (DED 1563 Tu./5006 Ta., generalizes to Ma./Ka./Te.); (2) `_OPT_HEADWORD_QUALIFIER` skips bare `?` (DED 4143 Tu.); (3) Pattern C `</b>\.?</i>` period-outside-bold (DED 3918 Koḍ); (4) `Koḏ.`(U+1E0F)→`Koḍ.` alias for To.-type sci-name shape (DED 2690). Isolated diff 15 changed/0 loss/+18 attest (10 bonus); +7 (19053→19060), No 143→136, Language-only frozen 158, 0 regressions; ledger key `parser-oneoff-noncanonical-markup-batch`. **Remaining scattered-parser tail still-No: 4002 Ta comma-marker, 379 Muria/Go segment+Elwin, 3535 Konda citation-in-italic, 4885 Konda (BB), 2116 Naiki plaintext-hw, 4473 Malto Malt;-typo**). Prior **98.4%** (19,053) after 2026-08-20 **Naiki (Ch.) between-marker-qualifier relabel** (5b4b16d, dravidilex-pilot, pushed; **PARSER, CORPUS REGEN'D — RE-SYNC TO BOX**: `burrow_entry_parser._find_all_lang_spans` folds a dialect qualifier sitting BETWEEN the marker and headword (outside the `<i>`) into the abbrev when `_is_known_qualified_abbrev` — shape `<i><b>Nk.</b></i> (Ch.) <b>hw</b>` stored `Nk. (Ch.)`=Naiki as bare `Nk.`=Naikri; guard airtight, Nk.(Ch.) is the only recognised between-marker composite so Go.(Tr.)/Ga.(S.)/(LSI…) untouched; isolated regen diff exactly 4 relabels Nk.→Nk.(Ch.), 0 loss; +3 (19050→19053), No 146→143, Language-only frozen 158, 0 regressions; ledger key `naiki-ch-between-marker-qualifier`; from unmatched_diagnostics.md. **Diagnostics file now ~worked out — matcher levers all shipped; remaining still-No parser_miss are scattered one-off HTML shapes (Tulu 1563/4143, Kodagu 2690/3918, Konda 3535/4885, Malto 4473, Tamil 5006/4002, Muria 379, Naiki 2116) with no shared lever; NEXT = per-row genuine_divergence logging or pick off the parser one-offs**). Prior **98.4%** (19,050) after 2026-08-20 **unterminated-paren comma-strip** (42edaaa, dravidilex-pilot, pushed; matcher-side/zero-regen: `textnorm.normalize_for_match` made the closing `)` OPTIONAL in the intra-parenthetical comma-strip so a truncated headword with a dangling `(` is handled — Burrow's DSAL markup sometimes closes the bold span mid-parenthetical (`<b>āgu (ān-, āy-</b>, etc.)`) so the parser stores an unbalanced-paren form (`āgu (ān-, āy-`) whose internal comma survived normalization and broke the substring match; balanced parens byte-identical; +7 all→Yes (19043→19050), Language-only 165→158, No frozen 146, 0 regressions; ledger key `unterminated-paren-comma-strip`; recovered DED 333/530 Kannada, 4687 Kodagu, 4778 Brahui, 4572 Kodagu, 2781 Ollari Gadba, 3098 Tamil; sized by counting unmatched rows whose `Matched Burrow form` had more `(` than `)` (12); residual genuine: 4968 Durg vowel, 3655 Kota/Toda bare-`n`+bracket, 5270 Durg, 4572 Tamil gloss-leak; **systemic-fold phase confirmed ~exhausted — Language-only now spread across ~46 langs, mostly genuine divergences**). Prior **98.4%** (19,043) after 2026-08-20 **multi-attestation inline-form scan** (1a04774, dravidilex-pilot, pushed; matcher-side/zero-regen: `starling_tree_validator._match_entry`'s inline-dialect-form scan (`gloss_dialect_exact`/`_substring`) previously inspected only `best_att`, missing an inline form living in a **sibling** Go. corpus segment; now iterates every language-matched attestation, best_att first then siblings in Burrow order; **CLOSES the prior NEXT LEAD**; resolves DED 1850 Adilabad `gudd- (Su.)`, 410 Adilabad `vēr`/`isar gunḍi` (2 rows), 4083 Muria `pīplī`, 990 Koya `onḍ`; Language-only 175→165, +10 all→Yes, No frozen 146, 0 regressions; ledger key `inline-form-scan-all-attestations`). Prior **98.341%** (19,033) after 2026-08-20 **TWO-FIX Adilabad session** (dravidilex-pilot, both pushed): (1) **ASu. inline sigil** (1f06a1a, matcher-side/zero-regen: `GONDI_INLINE_ABBREVS` += `ASu.`→Adilabad Gondi, the `(ASu.)` counterpart to shipped SR. fix a3de7c0; Burrow tags Adilabad forms `(ASu.)`=Adilabad Su. in the consolidated Go. gloss, 170 occ always Gondi context; Adilabad Language-only 22→5, +17, No frozen 146, 0 regressions); (2) **200-char gloss-cap removal** (a866dbe, **PARSER, CORPUS REGEN'D — RE-SYNC TO BOX**: dropped `gloss=gloss_text[:200]`; `_extract_gloss` already bounds the gloss so the cap only chopped in-span inline sub-forms past 200 chars, e.g. DED 513 `(ASu.) ḍiyyōr`; regen attest counts identical 30,334, 252 glosses grew + 1 shrank DED 5202 Koḍ.→`id.` = CORRECTNESS GAIN via A′ multiform detect; +12 incl. 513, No frozen 146, 0 regressions). **NEXT LEAD: multi-Go-segment/best_att-only scan** — residual 1850 `gudd-`/410 blocked because `(ASu.)` sits in a sibling Go. corpus entry the matcher doesn't scan (also blocks Muria 4083, Koya 990); 495 `velkī` logged genuine_divergence. **Systemic-fold phase ~exhausted otherwise — remaining Language-only mostly genuine divergences (Muria/Kuwi-Fitz/Inscr.Telugu ṛ-ḍ).** Prior **98.2%** (19,004) after 2026-08-20 **Konda glottal-trailing-space fix** (9b45580, matcher-side/zero-regen: re.sub('ʔ +','ʔ') after GLOTTAL_FOLD; Burrow source has space after modifier glottal 'muˀ er' vs Starling 'muʔer'; +8 matched Konda 5/Kuwi-Israel 1/Konda-B&B 1/Manda 1, No frozen 146, 0 regressions). Prior **98.2%** (18,996) after 2026-08-20 **Kui derivation-paren + HTML-entity fix** (99a1b69, two-part: matcher-side drop of '(< ...)' etymology parens + parser-side html.unescape of 18 headwords, **CORPUS REGEN'D — re-sync to Box**; Kui Language-only 12→6, +7 matched Kui 6/Pengo 1, No frozen 146, 0 regressions; residual 6 Kui genuine; next leads Muria Gondi 10/Kuwi-Fitz 8/Konda 7). Prior **98.1%** (18,989) after 2026-08-20 **Telugu optional-infix-paren expansion** (06482a6, matcher-side/zero-regen: normalize_for_match_variants expands Burrow X(Y)Z={XZ,XYZ} infix parens, e.g. k(r)ovvu; Telugu Language-only 41→7, +37 all→Yes, No frozen 146, 0 regressions; residual 7 = genuine divergences). Prior **97.9%** (18,952) after 2026-08-20 **Toda consonant-fold** (f8ab3fc, matcher-side/zero-regen: TODA_CONSONANT_FOLD folds Starling ɫ/ʒ + Burrow ł/ζ → l/z; Toda Language-only 57→9, +48 all Toda→Yes, No frozen 146; residual 9 = genuine om/em + separate ɵ↔q fold; next Language-only lead Telugu 41). Prior **97.7%** (18,904) after 2026-08-19 **Adilabad-Gondi SR-sigil fix** (a3de7c0, matcher-side/zero-regen: `GONDI_INLINE_ABBREVS` gained `SR.`→Adilabad Gondi — Burrow tags Adilabad forms `(SR.)` in the consolidated Go. gloss, e.g. DED 910 `(SR.) yeḍung`; Adilabad Language-only 62→22, +40 matched, 0 regressions; `Su.` excluded — collides with Koya `(Koya Su.)`; 22 residual = genuine divergences). **Triage method note: the triage_mismatches queue (~3000) is mostly already-matched "issue" rows; real unmatched = Match `No` (146) + `Language only` (304) — pattern-hunt within THAT. Next concentrated Language-only leads: Toda 57, Telugu 41.** Prior match rate **97.5%** (18,864) after 2026-08-19 **diagnostics-driven batch** (dravidilex-pilot, **8 commits pushed** from `unmatched_diagnostics.md`, +95 matched, 0 net regressions): df1349f paren-comma-strip (+5), bb89170 abbrev-alias-trailing-period (+3), 52ac72a slash+IPA-fold, cf29542 **plaintext-running-text-markers** (parser regen, +37), c799d51 sciname-bolded-abbrev (parser regen, +7), 791c65b **koya-spelled-out-marker** (+25 all Koya Gondi), ef0538b muria-MuE./MuW. (+3), 37bb56b kuwi-diacritic/page-markers (+5). **CORPUS REGEN'D (commits 4,5) — re-sync to Box.** Also logged 9 Irula genuine_divergence rows; user chose matcher-side Kuwi over the structural split; 2 attempts reverted for regressions (Muria postposed backward-look, Kuwi paren-attach). Prior **97.0%** (18,769) after 2026-08-18 **category-A abbrev/base-name batch** (178ed8d, matcher-side dialect_mapping.py, zero-regen, **found via `--primary language` triage**): new `_ABBREV_ALIASES` folds Burrow's stray orthography onto canonical keys — plain-d `Mand.` (DED 34)→`Manḍ.`/Manda, period-less `Koḍ` (DED 215/2826/5297)→`Koḍ.`/Kodagu — plus `Ir.` base `Iruḷa`→`Irula` (Starling uses plain-l Irula 33×, retroflex 0×; DEDR `Iruḷa` kept as baked corpus display); +6 matched, total 19,354 frozen = 0 regression; ledger key `abbrev-orthography-variants`; pushed. Prior **96.9%** (18,763): (−1) **spacing-diacritic-strip** (872da4f: SPACING_DIACRITICS strips Burrow's spacing nasal/breve modifiers ˜ U+02DC / ˘ U+02D8 to match Starling's NFKD-stripped combining marks; matcher-side/zero-regen; +23 across Telugu 12/Kurukh 4/Parji/Betul-Gondi/Manda/Maria-Gondi/Kolami, 0 regression); (0) **glottal-stop-fold** (852c9dc: GLOTTAL_FOLD folds ʔ U+0294 / modifier ˀ U+02C0 / apostrophe ’ → sentinel ʔ in normalize_for_match; matcher-side/zero-regen; Kurukh Language-only 26→6, Kuwi Israel 28→10, Maria Gondi 15→6; +67 matches, No/N-A frozen = 0 regression); (1) **hyphen-normalization fix** (630f65a: `normalize_for_match` deleted an internal citation hyphen instead of spacing it — Brahui `hamp-ing` vs Burrow `hamping`; matcher-side/zero-regen; Brahui Language-only 107→5, +115 matches, No/N-A frozen = 0 regression); (2) **subgroup-DB orphan exclusion** (ad5da93: the 653 "no DED number" N/A orphan rows all come from non-DED-keyed subgroup DBs cross-linked from dravet — gndet/Gondi-Kui 635 Konda, kuiet 5, telet 4, ndret 4, braet 3, kogaet 1, gonet 1 — **verified against live Starling pages**; validator now gates orphan-reporting on node `_url` basename via `_is_subgroup_db_source`, counts them in `summary.subgroup_db_orphans_excluded`; match rate unchanged 96.5% since these had ded_number=None/never in denominator; total_language_entries 20,007→19,354, N/A queue rows 653→0); prior 95.9%/18,558 **language-shape 4-lever batch** (1f9bf85: Pattern C3 leading-qual + citation-glued/`(LSB` Belari/Kurumba + sci-name bold-order + sci-name running-text w/ lexical guard; +110 attestations / +43 matches / 0 regressions; method = group No rows by language×HTML-shape from the audit xlsx; **CORPUS REGEN'D — re-sync to Box**); prior 95.7%/18,515 leading-qualifier-`<i><b>` (57e038d, Patterns C/C2, +42 matches, extends 32aeb26); prior 95.4%/18,473 To.-type bold-embedded-marker parser fix (44287fc, +221 attestations behind scientific names / +63 matches / 0 regressions, closes deferred To.-type plain-text variant); prior 95.1%/18,410 bare-letter-DED-suffix fold (02b1fa2, 3621A etc. → base key, +29, matcher-side/zero-regen, generalizes 4896(a)/(b)); 95.0%/18,381 zh-fold(ẓ/r̤ RESOLVED, +~190)+Pattern-G(DED 1 Ta.)+vowel-headword batch (fae4cca/fc3104b, pushed); ch/c fold declined; prior 94.0% after 2026-08-16 six-fix batch (Sonnet subagent; entries_matched 17,080→18,184, +1,104, biggest lever = gloss_secondary in-gloss form scan; also paren-aware headword split, pseudo-language fields skip, ɨ fold, Seoni "S." sigil, scraper spacing; committed `ff6370b`, pushed); 36 Yes→Language-only flips all explained (29 = pending ẓ/r̤ ruling); canonical-transcription policy 2026-08-16 (DEDR canonical, conservative core fold, build-layer swap deferred); **2026-08-16 corpus-dup reconciliation (loader-side, 94.0% held): dup-scrape attestation dedup (988 folded, incl. DED 137) + Appendix page-mislabel skip (10 entries pp.509+, DED(S) override bug) — both DONE, verified 0-regression; DED 410/4896 dedup subsumed**; user drives manual triage; **2026-08-17 id.-expansion over-glue RESOLVED (option A′ = leave ambiguous id. unresolved): 192 multiform id. refs gold-labeled, B head-meaning fabricated 26% wrong so shipped honest A′ via textnorm.antecedent_is_multiform() in both resolvers; regen 202 glosses→literal id., 94.0% held, +1 matched, 0 lost, 3 Match changes all positive; uncommitted; TODO re-sync corpus to Box**; next: build-layer canonical join, ẓ/r̤+ch/c ruling, To.-type TEXT variant/Kui-sense-suffix, Phase 2 single-char filter
- [Refactor roadmap — cross-validation scripts](project_refactor_roadmap.md) — COMPLETE: 4 commits (484cf6c…fe18280), each byte-identical vs baseline; two-layer DED cleaning is deliberate (textnorm=validator/ledger, parser=scrape/corpus)
