# DravidiLex LRC Import

Files here are generated by `src/dravidian/scripts/build_dravidilex_import.py`.
Re-run the script after fixing anything upstream; do not hand-edit generated
outputs.

**State: pilot frozen for the AsiaLex submission.** Every file in this directory was last
regenerated **2026-08-23** by the Sources-citation reformat described under "Sources" below.
All figures on this page were re-verified against the payload on 2026-09-05.

> ⚠ **The 2026-08-23 build-script change is uncommitted.** `build_dravidilex_import.py` carries an
> unstaged edit (the Sources reformat) that produced every generated file here. It is newer than
> the tip of `dravidilex-pilot` (`08e14ae`, 2026-08-22). Commit it, or a fresh clone will not
> reproduce these outputs.

## Current Inputs

| File | Role |
| --- | --- |
| `data/dravidian/starling/output.xlsx` | Tree-structured StarlingDB export, one row per word, linked by `Parent Word ID`. Produced by `src/dravidian/notebooks/destructuring_of_scraped_json.ipynb`. |
| `data/dravidian/lrc_import/dravidilex_languages.csv` | Tracked language-tier source of truth across machines. The older `data/dravidian/three-tier-language tree.xlsx` is only a fallback if this CSV is absent. |
| `data/dravidian/burrow_ded/burrow_corpus.cleaned.json` | Scraped DEDR corpus used to build per-reflex DEDR source citations. |
| `data/dravidian/lrc_import/buck_tag_suggestions.csv` | Reviewed Buck semantic-field assignments for root etyma. |

## Generated Outputs

| File | What it is |
| --- | --- |
| `dravidian_starling_data.xlsx` | Full normalized Starling tree, currently **30,823 rows** after dropping 563 empty Proto-North Dravidian placeholders. Protoform rows carry propagated DED numbers from their subtree. |
| `dravidilex_languages.csv` | Normalized `Family,Subfamily,Language` rows for the Laravel import command, currently **38 normal languages**. This file is both tracked input and rewritten normalized output. `Proto-Dravidian` is intentionally not listed here because the platform already has a built-in protolanguage page/link for it. |
| `dravidilex_batch_import.json` | Current import payload, **30,823 records**: **2,211** etyma/root rows and **28,612** reflex rows. 71.9 MB. |
| `dravidilex_batch_import.xlsx` | Human-reviewable spreadsheet of the same import payload. |
| `batched/` and `batched_compat_lrctest/` | Historical/admin-upload chunks. The current path is the Artisan import command, not browser batch upload. |

Current source attachment coverage in `dravidilex_batch_import.json` (verified 2026-09-05):

- `STARLING`: 27,789
- `DEDR`: 27,673
- `CVOTGD`: 662

**Root/etymon StarlingDB permalinks — recovered 2026-08-22** (`08e14ae`, with
`crawl_root_permalinks.py` / `crawl_root_corpus.py` / `match_root_stragglers.py`).
**2,122 of 2,211 roots (96.0%)** now resolve to their own Starling record via
`data/dravidian/starling/dravet_root_permalinks.json` instead of falling back to the nearest
reflex's record. The remaining 89 roots keep the fallback.

Current build-shaping guarantees:

- `HeadwordEntries` exists on all 28,612 reflex rows so Laravel does not
  comma-split raw `Headwords`.
- `EtymonEntry` exists on all 2,211 root rows, already asterisk-free for LRC
  etymon display.
- `Additional Forms` is normalized to `Additional forms`; regenerated JSON
  should have `Additional forms = 2,518` and `Additional Forms = 0`. ✅ verified
  2026-09-05: 2,518 / 0.
- Root/etyma rows carry Starling provenance as `Source (StarlingDB)` extra data.
  Roots do not carry `Sources` because the platform has no `lex_etyma_source`
  table.

## Mapping Decisions

- **Starling dialects -> tree languages.** Starling splits dialect/source
  varieties that the import language tree does not. Each is mapped to its tree
  language for LRC (`Gondi` dialects -> `Gondi`; Kuwi source names -> `Kuvi`;
  `Ollari Gadba` -> `Ollari`; other Gadba varieties -> `Gadaba`; etc.). The
  original Starling language name is preserved as `Language (Starling)`.
- **Kasaba stays distinct** (Todd, 2026-07-05: err toward granularity on the
  first pass). It has its own language row under South / Proto-South Dravidian I.
- **`Proto-North-Dravidian`** is normalized to `Proto-North Dravidian`.
- **Empty North Dravidian placeholders are dropped.** The scrape emitted 563
  empty `Proto-North Dravidian` placeholder rows with no DED number, URL,
  reflexes, or distinguishing data. The build drops them before language-name
  normalization, preserving the real hyphenated rows and avoiding duplicate
  headword IDs.
- **Intermediate proto-languages** are explicit language rows:
  `Proto-South Dravidian`, `Proto-Central Dravidian`,
  `Proto-North Dravidian`, `Proto-Nilgiri`, `Proto-Telugu`,
  `Proto-Gondi-Kui`, `Proto-Gondi`, `Proto-Kui-Kuwi`,
  `Proto-Pengo-Manda`, and `Proto-Kolami-Gadba`. The top-level
  `Proto-Dravidian` is not a normal language row; all `Proto-Dravidian`
  records are root etyma, and the sidebar already gets its
  `/language/protolanguage` link from `lexicon.protolang_name`.

## Current Import Runbook

**The import command is merged upstream.** `app:import-dravidilex-csv` landed on
`cola-laits/linguistics_research_center` **master on 2026-08-23** as PR **#203**
(`932a16abd` → merge `102e07836`). Two companion PRs merged the same day:
**#202** advanced-search columns + Telugu/Spanish/English i18n (`450593b96`), and
**#201** source-citation HTML rendering (`935bb4491`) — the blade change that makes the
`Sources` HTML below render instead of escape.

The command creates the lexicon, languages, Buck semantic categories/fields,
2,211 etyma with semantic-field links, 28,612 reflexes, reflex-to-etymon links,
and reflex `Sources`. It **does not create landing pages or language pages** — those were
deliberately kept out of the import-command PR.

```bash
# JSON must be readable inside the container; for local Docker, copy it under server/ first.
cp /path/to/dravidilex_batch_import.json linguistics_research_center/server/storage/app/
docker compose exec web php artisan app:import-dravidilex-csv storage/app/dravidilex_batch_import.json
docker compose exec web php artisan app:generate-lexicon-data-cache <printed lexicon id>
```

For lrc-test/prod: the PRs are merged, so what remains is **one server-side run by Chris**
against the current `dravidilex_batch_import.json`, then a lexicon-data-cache regeneration.
Open question for Todd: fresh `dravidilex` slug (recommended) vs. teardown of the existing
`dravidilex_pilot`.

## Sources

`Sources` is emitted only for reflex rows and consumed by the Laravel import
command into the `lex_reflex_source` pivot:

- `DEDR`: full cleaned Burrow entry HTML by the reflex's own `Number in DED`.
- `STARLING`: compact rich citation with Starling database/text number/record/link.
- `CVOTGD`: page/entry-number-only. **TODO — CVOTGD has still not been scraped**: `original_entry`
  is empty on all 662 rows and only `page_number` is populated. This is the one open data gap in
  the payload. (The other open TODO on the pilot is not a data gap but a review one: a human
  spot-check of the `genuine_divergence` set behind the validation figure — see
  `docs/dravidian_validator_progress.md` §11.)

**Citation format — reformatted 2026-08-23** (`build_starling_entry`). The database label is now
italic, the record id bold, the parts comma-joined, and the link follows on the same line:

```html
<small><i>South Dravidian etymology</i>, text no. 42, record <b>PSD001</b>.
<a href="https://starlingdb.org/cgi-bin/response.cgi?...">View original record</a></small>
```

The previous shape used `·` separators and a `<br>` before the link. Nothing else about the
payload changed in that regeneration.

Source HTML is allowlist-sanitized in `build_dravidilex_import.py` before it
enters the JSON (`a`, `b`, `br`, `i`, `small`, `sup`). The Laravel blade now renders
`original_text` as HTML (PR #201, merged 2026-08-23), so these citations display as intended.

## Semantic Tags

Semantic categories are no longer just flat extra data in the current import
path. The Laravel command seeds Buck categories/fields and creates
`LexEtymaSemanticField` rows for the 2,211 root etyma from
`Semantic Tag (Buck)`. This is what populates the Categories tab and the
Advanced Search `semantic_tag` column for reflexes linked to etyma.

Historical flat/browser-upload paths can still display the Buck tag as root
extra data, but they do not create real etyma or semantic-field links.
